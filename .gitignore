.coolify.env
__pycache__/
bootstrap_old.json
my_dashboard.html
# localhost-only preview of the real squad - never publish (repo is public)
my_team_preview.json
# generated by the news sweep on boot
news_cache.json
# momentum snapshots (accumulated locally / on a persistent volume)
data/
optimal_squad.json
